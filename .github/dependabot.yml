# Keeps the root requirements.txt dependencies fresh for the Community Cloud demo.
# All updates are batched into one daily PR to keep the noise down for a template repo.
version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"
    groups:
      python-dependencies:
        patterns:
          - "*"
